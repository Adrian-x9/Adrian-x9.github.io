// Plik: config-about.js (NOWA, UPROSZCZONA WERSJA)

// 1. Konfiguracja ścieżek dla tej podstrony
const config0 = {
  pageSettings: {
    pathCorrection: "..", 
  }
};

// 2. Konfiguracja specyficzna dla tej podstrony
const config2 = {
  pageSettings: {
    pageKey: "about",
    pageTitle: "O systemie VisuDir",
    showBackButton: true,
    defaultBgVideoFile: 'green-34.mp4', 
  },
};